# 個人情報の取り扱いについて（連絡先E2E暗号化の仕組み）

本システムの運営者（ホスティング事業者）が**作業者の連絡先を閲覧できない**ことを、
技術的に担保するための仕組みを説明します。

## 保持する個人情報の整理

| データ | 保存形態 | 運営者から見えるか |
|--------|---------|------------------|
| 氏名・社員ID | 平文 | 見える（シフト運用に必須） |
| 電話番号 | **E2E暗号化**（暗号化有効時） | **見えない**（暗号文のみ） |
| メールアドレス | **保持しない**（カラム自体を削除済み） | — |
| パスワード | bcryptハッシュ | 見えない（不可逆） |

## E2E暗号化の仕組み

```
管理者ブラウザ                     サーバ（運営者）
┌────────────────┐              ┌────────────────┐
│ 090-1234-5678  │──暗号化──▶  │ enc.v1.xxxx…   │
│      ▲         │              │ （暗号文のみ）   │
│      └─復号     │◀──取得────  │                │
│ 鍵 = パスフレーズ │              │ 鍵を持たない     │
└────────────────┘              └────────────────┘
```

1. テナント管理者が「作業者管理」画面で**パスフレーズ**を設定する
2. ブラウザ内で PBKDF2（SHA-256, 310,000回）により AES-256-GCM 鍵を導出する
3. 電話番号は**管理者のブラウザ内で暗号化されてから**サーバに送信される
4. サーバ（DB）には `enc.v1.<iv>.<暗号文>` 形式の暗号文のみが保存される
5. 表示時も管理者のブラウザ内で復号する。**パスフレーズ・復号鍵がサーバに送信されることはない**

サーバが保持するのは以下のみで、いずれからも電話番号は復元できない:

- KDFソルト（秘密情報ではない）
- 検証用暗号文（パスフレーズの正誤判定用。既知の定数を暗号化したもの）

## 運営者ができないこと

- DBファイルを直接開いても、電話番号は暗号文しか見えない
- サーバのアクセスログ・アプリログに連絡先が記録されることはない
  （QRログイントークンも同様にログからマスクされる）

## 制約・注意事項

- **パスフレーズを忘れると連絡先は復元できません。** その場合は暗号化設定を
  リセットし、全作業者の電話番号を再入力する必要があります（他のデータは無事）
- 復号鍵はブラウザのタブセッション中のみ保持されます（タブを閉じると再入力）
- 暗号化・復号は管理者の端末上でのみ行われるため、作業者画面の動作には影響しません
