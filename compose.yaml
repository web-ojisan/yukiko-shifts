services:
  app:
    build: .
    ports:
      - "8989:8989"
    volumes:
      - ./data:/app/data
    environment:
      JWT_SECRET: "local-dev-secret-32chars-minimum!!"
      TZ: Asia/Tokyo
      DEMO_LOGIN: "true"  # 開発用。本番では設定しない
    restart: unless-stopped
